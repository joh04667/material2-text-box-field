import { MdTextFieldDirective } from './md-text-field.directive';

describe('MdTextFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new MdTextFieldDirective();
    expect(directive).toBeTruthy();
  });
});
